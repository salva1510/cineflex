CINEFLEX BUILD 6.1.1 — PLAYER FULLSCREEN FIX

FIXED:
- Restored full-size 16:9 video player.
- Server tabs no longer consume iframe height.
- Restored Fullscreen button on mobile.
- Added native fullscreen toggle.
- Added fixed-screen fallback for installed PWA/mobile browsers.
- Fullscreen now uses the entire viewport.
- Preserved multi-server engine and all Build 6.1 features.

INSTALL:
Replace all files, clear site data/cache, then reopen CineFlex.
For installed PWA, close and reopen it. Reinstall only if the old service worker remains cached.

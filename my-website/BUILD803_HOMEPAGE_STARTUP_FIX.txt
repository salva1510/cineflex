CINEFLEX BUILD 8.0.3

Fixed homepage stuck on CineFlex logo.
Cause: the Add Time MutationObserver called build() for every DOM mutation, while build/render also changed the DOM, creating an infinite loop.

Changes:
- Safe conditional observer
- 120ms debounce
- UI repair runs only when an Add Time control is genuinely missing
- Add Time and Add Profile fixes retained
- Asset/cache versions bumped to 8.0.3

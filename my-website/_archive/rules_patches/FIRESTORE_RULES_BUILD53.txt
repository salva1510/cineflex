// Merge these match blocks inside your existing Firestore rules service/cloud.firestore block.
// Keep your current Watch Party, comments, ratings, profiles and admin rules.

match /cineflex_reactions/{titleKey} {
  allow read: if true;
  allow create, update: if request.auth != null;
  allow delete: if false;

  match /users/{uid} {
    allow read: if request.auth != null;
    allow create, update, delete: if request.auth != null && request.auth.uid == uid;
  }
}

match /cineflex_activity/{activityId} {
  allow read: if true;
  allow create: if request.auth != null
    && request.resource.data.uid == request.auth.uid;
  allow update, delete: if request.auth != null
    && resource.data.uid == request.auth.uid;
}
